//PROGRAM TO PRINT SUM OF N EVEN NUMBERS USING RECURSIVE FUNCTION//
#include<stdio.h>
#include<conio.h>
int sum(int x);
main()
{
int n;
scanf("%d",&n);
int s=sum(n);
printf("%d",s);	
}
int sum(int x)
{
	if(x==0)
	return 0;
	else
	return x+sum(x-2);
}
